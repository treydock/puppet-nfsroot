---
:backends:
  - yaml
:hierarchy:
  - "fqdn/%{::fqdn}"
  - "fqdn/%{::fqdn}/data"
  - "hostgroup/%{::hostgroup}/%{::osc_rw_type}"
  - "hostgroup/%{::hostgroup}"
  - "hostgroup/%{::hostgroup}/data"
  - "hostgroup/%{::hostgroup_parent}"
  - "hostgroup/%{::hostgroup_name}"
  - "%{::osfamily}%{::operatingsystemmajrelease}"
  - common
  - data/owens_hosts
  - data/ruby_hosts
  - data/oakley_hosts
  - ../site_modules/profile/spec/fixtures/data/accd_project_data
:yaml:
  :datadir: 'spec/fixtures/modules/hieradata'
:merge_behavior: deeper
